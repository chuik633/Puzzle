let time_mode = 'time'

function set_time_mode(mode){
    console.log("setting time mode", mode)
    if(mode =='first-attempt'){
        time_mode == 'time'
    }else{
        time_mode = mode
    }
    console.log('time mode', time_mode)
   
}

let people_data = []

function set_people_data(people_data){
    people_data=people_data
}

